  
Aktienprogramm Niederhauser Daniel:
Mithilfe dieses Programmes ist es möglich, Aktienwerte auszulesen und zu verarbeiten.
Es findet zuerst ein API-Aufruf statt, wo die alle Aktienwerte eines vom Benutzer ausgewählten Unternehmens 
abgerufen werden und dann in der Datenbank "Java" in einen eigenen Table gespeichert werden (Falls der Table noch nicht existiert,
wird er erstellt). Dann wird der Benutzer gefragt, ob er sich den Table ausgeben lassen will. Nachdem gibt der Benutzer den gewünschten Gleitdurchschnitt z.B. 200 an.
Anschließend gibt der Benutzer noch die gewünschte Datenmenge an, welche er danach sehen will z.B. 500.
Abschließend wird noch die Grafik angezeigt und wenn der letzte Close-Wert größer, als der aktuelle Gleitdurchschnitt ist, grün eingefärbt, und wenn er kleiner ist, rot. 


